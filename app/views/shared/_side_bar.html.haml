-# <!-- トップ画面（サイドバー）の部分テンプレート -->

.wrapper
  .chat-side
    .chat-side__header
      .chat-side__header__box
        .chat-side__header__box__user-name
          = current_user.name
      .chat-side__header__box__menu
        .chat-side__header__box__menu__new-group
          = link_to new_group_path do
            =fa_icon "edit"
        .chat-side__header__box__menu__edit-user
          = link_to edit_user_path(current_user) do
            =fa_icon "cog"
       
  .chat-side__group
    - current_user.groups.each do |group|
      .chat-side__group__list
        = link_to group_messages_path(group) do
          .chat-side__group__list__group-name
            = group.name
          .chat-side__group__list__latest-message
            メッセージはまだありません。






